# core

This folder contains the core implementation of the low-latency limit order book.
It holds the engine logic, headers, and supporting source files that power the
main matching and order management behavior.
