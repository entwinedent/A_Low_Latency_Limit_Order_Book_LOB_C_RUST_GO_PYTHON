# cmake

This folder contains shared CMake modules and build configuration helpers.
It centralizes reusable build logic for compiling the engine, benchmarks, and
language bindings consistently across platforms.
