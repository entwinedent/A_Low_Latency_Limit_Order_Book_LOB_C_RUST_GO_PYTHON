# bindings

This folder contains language bindings for the core engine.
It provides integration surfaces for Go, Python, and Rust so the order book
functionality can be used from multiple runtimes and ecosystems.
