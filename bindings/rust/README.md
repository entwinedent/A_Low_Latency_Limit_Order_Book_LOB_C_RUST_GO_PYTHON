# rust

This folder contains the Rust bindings and FFI integration layer for the engine.
It provides the Rust-side wrappers and bridge code needed to call into the core
order book implementation from Rust.
